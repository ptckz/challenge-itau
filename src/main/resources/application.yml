spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:db
    username: sa
    password: 
  jpa:
    show-sql: true 
    hibernate:
      ddl-auto: update
    open-in-view: true

server:
  port: 9000
  error:
    include-stacktrace: never
  servlet:
    context-path: /
